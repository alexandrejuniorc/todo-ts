import React from 'react';
import '../scss/Header.scss';

const Header = () => {
  return (
    <header className="header">
      <h1>React + TS Todo</h1>
    </header>
  );
};

export default Header;
